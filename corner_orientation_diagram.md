# 角块位置与扭转朝向示意

这个示意使用右前上角的角块 `UFR`。字母代表贴纸所属的中心面：
`U` 为上面、`D` 为下面、`F` 为前面、`B` 为后面、`R` 为右面、`L` 为左面。

角块的 **位置** 表示它在哪一个角槽中；角块的 **扭转朝向** 则只关心它的
`U` 或 `D` 色贴纸落在哪一类面上。后者不是完整的三维旋转角度。

## 1. 初始状态：UFR 角块

下图只高亮右前上角的三张贴纸。`[U]`、`[F]`、`[R]` 是同一颗角块的贴纸。

```text
                    +---------+
                   /         /|
                  /   [U]   / |
                 +---------+  |
                 |         |  |
                 |         | [R]
                 |         |  |
                 |   [F]   |  +
                 |         | /
                 +---------+/

                 上面 U      前面 F     右面 R
                 角块位置：UFR
                 朝向值：0
```

`U` 色贴纸在 `U` 面，因此此角块处于“未扭转”的编码状态 `0`。

## 2. 执行 U 顺时针：位置变了，但没有扭转

从上面看，顺时针转动上层。原来的 `UFR` 角块移动到左前上角 `UFL`。

```text
       从 U 面向下看

       转动前                         U 顺时针后

          F                                R
          ^                                ^
          |                                |
      +---+---+                        +---+---+
      | UBL | UBR |                    | UFL | UBL |
  L <-+-----+-----+-> R            F <-+-----+-----+-> B
      | UFL |[UFR]|                    |[UFR]| UBR |
      +---+---+---+                    +---+---+---+
          |                                |
          v                                v
          B                                L

       被标记的角块：UFR -> UFL
```

移动后从左前上角看该角块：

```text
                    +---------+
                   /         /|
                  /   [U]   / |
                 +---------+  |
                 |         | [F]
                 |         |  |
                 |   [R]   |  |
                 |         |  +
                 +---------+/

                 [U] 仍在上面 U
                 [F] 从 F 面转到 L 面
                 [R] 从 R 面转到 F 面
                 角块位置：UFL
                 朝向值：0
```

虽然 `[F]`、`[R]` 等侧面贴纸会随着角块移动到新的侧面，`[U]` 仍在 `U` 面。
因此它的三维位置和贴纸去向改变了，但角块朝向增量为 `0`。这就是 `_MOVE_TABLES["U"]`
的后半部分全为 `0` 的原因。

## 3. 执行 R 顺时针：位置变化，同时发生扭转

按这张表所使用的常见角块编号（`UFR` 是索引 `0`，`UBR` 是索引 `3`），右面
转动会让 `UFR` 移到 `UBR`。此时原来朝上的贴纸被带到后侧。右图改从后上方
略偏右观察，因此 `B`、`R`、`U` 三面都可见：

```text
             R 顺时针前                         R 顺时针后

                    +---------+                       +---------+
                   /         /|                      /   [F]   /|
                  /   [U]   / |                     +---------+ |
                 +---------+  |                     |   [U]   |[R]
                 |         | [R]                    |  B 面    | R 面
                 |         |  |                     |         |  +
                 |   [F]   |  +                     |         | /
                 |         | /                      +---------+/
                 +---------+/

                 角块位置：UFR                     角块位置：UBR
                 [U] 在 U 面                        [U] 在 B 面
                 朝向值：0                           朝向值：1
```

这里的 `1` 来自 `_MOVE_TABLES["R"]` 后半部分中目标位置 `UBR`（索引 `3`）的值。
它**不表示这一步把角块实际旋转了 120 度**：`R` 面本身只旋转 90 度。`1` 是一个
状态标签，表示这个角块的 `U/D` 色贴纸现在落在 `F/B` 这一类侧面。

## 4. 这张表使用的精确规则

采用常见的角块位置顺序：

```text
索引：  0    1    2    3    4    5    6    7
位置： UFR  UFL  ULB  UBR  DFR  DLF  DBL  DBR
```

一个角块的朝向值由它的 `U` 或 `D` 色贴纸所在的面分类，而不是由观察者看到它转了
多少度分类：

```text
朝向值  U/D 色贴纸所在的面
------  --------------------
0       U 或 D
1       F 或 B
2       R 或 L
```

移动表的第一组元组是“新位置从哪个旧位置取角块”，第二组元组是该新位置的朝向增量。
若旧状态为 `old_permutation`、`old_orientation`，则对每一个新位置 `i`：

```python
source_position = permutation[i]
new_permutation[i] = old_permutation[source_position]
new_orientation[i] = (old_orientation[source_position] + orientation_delta[i]) % 3
```

以 `R` 为例，目标位置 `3`（`UBR`）从旧位置 `0`（`UFR`）取走角块，且增量是 `1`：

```text
new_orientation[UBR] = (old_orientation[UFR] + 1) % 3
                         (0 + 1) % 3 = 1
```

从合适的 `B-R-U` 观察角度看，这正对应右图：原 `[U]` 贴纸在 `B` 面，原 `[F]`
贴纸在 `U` 面，原 `[R]` 贴纸仍在 `R` 面。用 `0/1/2` 并对 3 取模，是为了让连续
的面转动能够用简单加法组合；它不是对单步物理旋转角度的直接测量。

## 编码总结

```text
朝向值  U/D 色贴纸的位置
------  -----------------------------------
0       仍在 U 或 D 面：没有相对上下轴扭转
1       在 F 或 B 面：朝向状态 1
2       在 R 或 L 面：朝向状态 2
```

因此一个角块只需记录三种朝向。它有三张贴纸；固定角块所在的位置后，能让它嵌入该
角槽的有效姿态恰好有三种。三个姿态之间可以理解为相对角槽扭转 120 度，但面转动
过程本身仍然是物理上的 90 度旋转。
