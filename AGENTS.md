# Python 编码规范

## 类型与返回值

- 所有函数、方法和异步函数必须为每个参数和返回值添加类型标注。
- 不返回业务值的函数必须显式标注 `-> None`。
- 对可空值使用 `T | None`；避免用未注释的 `None` 表达可选类型。
- 公共 API 的复杂返回值应使用具名类型、`TypedDict`、`dataclass` 或 Pydantic 模型，避免返回含义不明的元组和字典。
- 不使用 `Any` 逃避类型检查；确有边界兼容需求时，限制其作用域并说明原因。

## 注释与文档

- 类和函数的说明注释放在定义语句正上方一行，并与定义保持相邻。
- 如果一个函数的功能分为明显的几个阶段，在每个阶段开始时添加注释。
- 不在类或函数体开头使用文档字符串替代该说明注释。
- 注释解释设计意图、约束或不明显的原因，不复述代码本身。
- 修改行为时同步更新相邻注释，删除过时说明。

## 代码组织

- 一个模块只承载一个清晰职责；按功能拆分，避免循环依赖。
- 通用模块直接放在 `src/<模块名>/`；除非存在明确的发布或隔离需求，不增加项目名称作为额外包层级。
- 导入顺序为：标准库、第三方库、项目内模块；各分组之间留一行空行。
- 使用绝对导入，避免通配符导入。
- 常量使用全大写，类使用 `PascalCase`，函数、变量和模块使用 `snake_case`。
- 优先使用 `pathlib.Path`、上下文管理器和标准库类型，避免手工资源管理与字符串路径拼接。
- 异常只捕获能够处理的具体类型；不要使用裸 `except:` 或静默忽略异常。
- 函数保持短小、职责单一；避免可变默认参数、隐式全局状态和副作用隐藏在工具函数中。

## 质量要求

- 新增或修改行为时，补充或更新覆盖该行为的自动化测试。
- 测试应独立、可重复且不依赖网络、真实密钥、当前时间或执行顺序。
- 运行格式化、静态检查和相关测试后再提交；不得通过删除测试或降低断言来掩盖问题。
- 机密信息只从环境变量或受管配置读取，不写入源码、测试、日志或示例。

## examples 目录

- 测试库用法、实验代码、概念验证、可运行演示和第三方 API 探索统一放入 `examples/`。
- `examples/` 中的代码不得被生产代码导入，也不得成为运行时依赖。
- 每个示例应自包含，注明所需依赖和运行方式，并使用占位符替代真实凭据。
- 正式单元测试与集成测试仍放在 `tests/`，不与示例混放。
