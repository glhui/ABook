# SciPy

- 先确认项目环境已声明 SciPy 依赖，并选择与任务匹配的子模块，例如 `scipy.optimize`、`scipy.integrate` 或 `scipy.interpolate`。
- 优化任务须检查收敛状态、输入维度和约束条件；不要只返回求解器结果对象而忽略失败状态。
- 数值算法测试使用固定输入与适当容差，断言可观察的数值结果和错误路径，不依赖平台偶然的迭代次数。
- SciPy 建立在 NumPy 数组约定之上；保持形状、dtype 和非有限值处理的一致性。
