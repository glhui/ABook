# NumPy

- 先确认项目环境已声明 NumPy 依赖；不要在任务中隐式安装包。
- 使用 `numpy.ndarray` 与数组形状约束表达数值输入，避免把 Python 嵌套列表当作不受约束的矩阵。
- 向量化计算优先于 Python 逐元素循环；矩阵乘法使用 `@` 或 `numpy.matmul`，不要混淆逐元素乘法 `*`。
- 浮点结果测试使用 `numpy.testing` 的近似比较，并覆盖形状不匹配、非有限值等需求明确的边界。
